/* let esEntero = false, numero;
    do {
        numero = prompt("Introduce un número entero");
        if (parseInt(numero) !== "NaN" && Number.isInteger(+numero)) { esEntero = true }
        else {
            esEntero = false;
            alert("Eso no es un número entero colega")
        }
    } while (!esEntero);*/


function hazAlgo () {
    alert('Hola');
}

function dameAlgo () {
    let numero = 5;
    return numero;
}

const pideNum = function pide () {
    respuesta = prompt("Dame un número");
    return respuesta;
}

function meDasTeDoy (num) {
    if (parseInt(num) === "NaN" && !Number.isInteger(+numero)) { alert("Eso no es un número entero colega") }
        else {
          if (num == 5) {alert("Acertaste mi número");
        } else { alert("Ese no es mi número");} 
            
        }
    } 


